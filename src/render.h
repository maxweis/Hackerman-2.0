#pragma once

#include "ofMain.h"

const ofColor kWhite = ofColor(255, 255, 255);
const ofColor kBlack = ofColor(0, 0, 0);
const ofColor kMainColor(0, 255, 0); 
const ofColor kAccentColor(10, 150, 10);

const int kLineWidth = 3;
const int kBorderWidth = 6;
